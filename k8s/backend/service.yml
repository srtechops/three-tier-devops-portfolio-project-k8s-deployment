apiVersion: v1
kind: Service
metadata:
  name: portfolio-backend-service
spec:
  type: NodePort
  selector:
    app: portfolio-backend
  ports:
    - port: 8080          
      targetPort: 8080    
      nodePort: 30081    
      protocol: TCP
